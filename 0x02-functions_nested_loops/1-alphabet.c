#include "holberton.h"
/**
 *print_alphabet - print alphabet
 *is not necessary the
 *return because only
 *prints in
 *terminal a
 *result in
 *this case
 *
 */
void print_alphabet(void)
{
char i;
for (i = 'a'; i <= 'z'; i++)
{
_putchar(i);
}
_putchar('\n');
}
